import 'package:flutter/material.dart';

class UserMedicationsPage extends StatelessWidget {
  const UserMedicationsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Medications Page',
        style: TextStyle(fontSize: 50),
      ),
    );
  }
}
