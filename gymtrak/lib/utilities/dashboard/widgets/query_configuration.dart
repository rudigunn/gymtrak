class QueryConfiguration {
  String? selectedDataSource;
  String? selectedEntry;
  String? selectedTransformation;

  QueryConfiguration({this.selectedDataSource, this.selectedEntry, this.selectedTransformation});
}
